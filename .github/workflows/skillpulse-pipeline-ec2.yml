name: Deploy to EC2 Server

on: 
  push:
    branches: [main]
  workflow_dispatch: 
 
jobs:
  CI:
    uses: ./.github/workflows/ci.yml
    secrets: inherit

  CD:
    needs: [CI]
    uses: ./.github/workflows/cd.yml
    secrets: inherit
