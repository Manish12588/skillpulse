name: Pipeline - Deploy to Self Hosted Runner

on: 
  #push:
  #  branches: [main]
  workflow_dispatch:
 
jobs:
  CI:
    name: Build and Push to DockerHub
    uses: ./.github/workflows/ci.yml
    secrets: inherit

  CD:
    name: Deploy to Self Hosted Runner
    needs: [CI]
    uses: ./.github/workflows/cd-self-hosted.yml
    secrets: inherit
